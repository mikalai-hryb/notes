const lessonsCount: any = 10;

function printCourse(title: string, lessonsCount: number) {
    console.log(`Title: ${title}, lessons count: ${lessonsCount}`)
}

const id: number | string = 1000
const keys: (number | string) [] = [1, 2, 3, "123"]
let courseId: number | null = 1000;
courseId = null;
