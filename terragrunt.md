# Terragrunt

* they try to follow DRY principle
* reduce code duplication between a set of the similar environments (inputs, outputs, providers, backend)
* backend configuration can be dynamic
* before and after hooks
* parallel runs for modules
